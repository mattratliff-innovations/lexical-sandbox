## Management commands:
   - Start:   sudo nginx -c {pwd}/spell-checker/nginx/nginx.conf
   - Stop:    sudo nginx -s quit
   - Reload:  sudo nginx -s reload
   - Test:    nginx -t -c {pwd}/spell-checker/nginx/nginx.conf